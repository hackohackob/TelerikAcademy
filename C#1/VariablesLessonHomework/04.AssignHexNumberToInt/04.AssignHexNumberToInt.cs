using System;

//Declare a variable of type int and assign it with 254 in hexadecimal format

class AssignHexNumberToInt
{
    static void Main()
    {
        int number=0xFE; //Declaring a variable and assinging it with 254 in Hex format (FE)
        Console.WriteLine(number); //Writing the value of the variable on to the Console in Dec format
    }
}

